# Override the backend target used by the Vite dev proxy for /api requests.
# Defaults to http://localhost:5091 (the ASP.NET Core "http" launch profile).
# VITE_API_PROXY_TARGET=http://localhost:5091
