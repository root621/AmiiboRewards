services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: amiibo_rewards
      POSTGRES_USER: amiibo
      POSTGRES_PASSWORD: amiibo
    ports: ["5432:5432"]
    volumes: ["postgres-data:/var/lib/postgresql/data"]
volumes:
  postgres-data:
